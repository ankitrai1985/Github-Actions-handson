  name: Consume Workflow main
  on: workflow_dispatch

  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout code
          uses: actions/checkout@v2

        - name: Call reusable workflow
          uses: ankitrai1985/Github-Actions-handson/.github/workflows/reusable_workflow.yml@main
          with:
            name: 'Ankit Rai'
            token: ${{ secrets.GITHUB_TOKEN }}
